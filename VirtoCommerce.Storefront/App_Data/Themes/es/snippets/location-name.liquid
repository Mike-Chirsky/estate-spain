{%- if collection.city_name -%}
    {{- collection.city_name -}}
{%- endif -%}
{%- if collection.region_name -%}
    {%- if collection.city_name -%}
        {{- ', ' -}}
    {%- endif -%}
    {{- collection.region_name -}}
{%- endif -%}
{%- if keyword != null and collection.region_name == null and collection.city_name == null -%}
    {{- keyword -}}
{%- endif -%}