{%- for property in product.properties -%}
    {%- if property.name == 'other_type' -%}
        {{- property.value | capitalize | append: ' ' -}}
    {%- endif-%}
{%- endfor -%}
{{-'product.number' | t -}}
{{- product.first_available_variant.sku | append: ' '-}}
{%- for property in product.properties -%}
    {%- if property.name == 'propertysquare' -%}
        {{- property.value -}}
    {%- endif -%}
{%- endfor -%}
{%- for property in product.properties -%}
    {%- if property.name == 'landsquare' -%}
        {{- property.value -}}
    {%- endif -%}
{%- endfor -%}
{{- 'product.square2' | t | append: ' ' -}} {{- 'product.in-city' | t | append: ' '-}}
{%- for property in product.properties -%}
    {%- if property.name == 'city' -%}
        {{- property.value -}}
    {%- endif-%}
{%- endfor -%}